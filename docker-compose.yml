services:
  jekyll:
    build: .
    ports:
      - "4000:4000"
    volumes:
      - .:/srv/jekyll
      - bundle:/bundle
    environment:
      - JEKYLL_ENV=development
    restart: always
    command: jekyll serve --host 0.0.0.0 --watch

volumes:
  bundle: